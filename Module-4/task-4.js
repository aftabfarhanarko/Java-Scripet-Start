

/*
4. Checking if it's an Array
Instructions:

Create different variables, each containing either an array or a non-array value.

Now use isArray to check if each variable is an array.

Print a message to the console indicating whether each variable is an array or not.*/

const vali = [1,2,3,4,5,5,6,6,7,7,787];
const array = "iufhjfbkjsbfi9oew wdh iuob ";
const kudhh = ["hjh", 'woiudh', 'oid3io', 'owqul.n','poie'];
const test = [11.2, 22.1, 33.2, 44.3, 55.4];
let oop = 4775;

console.log(Array.isArray(vali));
console.log(Array.isArray(array));
console.log(Array.isArray(kudhh));
console.log(Array.isArray(test));
console.log(Array.isArray(oop));